Install [Python3](https://www.python.org/downloads/) and [Chromium Webdriver](https://chromedriver.chromium.org/downloads), either through these links or via the command line:
```
sudo <packagemanager> install python3 python3-pip chromium-driver
```

Install required packages:
```
    pip install -r requirements.txt
    # or
    pip install keyboard selenium webdriver-manager
```

Run `evaluate.py` to launch the evaluation tool.